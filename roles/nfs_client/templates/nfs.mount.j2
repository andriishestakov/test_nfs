[Unit]
Description=Mount NFS {{ item.path }}
Requires=network-online.target
After=network-online.target

[Mount]
What={{ item.src }}
Where={{ item.path }}
Type=nfs
Options={{ item.opions | default(nfs_client_default_mount_options) }}

[Install]
WantedBy=multi-user.target
